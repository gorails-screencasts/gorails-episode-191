<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weekly_email %>
    <%= form.check_box :weekly_email %>
  </div>

  <div class="field">
    <%= form.label :playback_rate %>
    <%= form.select :playback_rate, [0.5, 0.75, 1, 1.5, 2] %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
